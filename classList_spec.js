/* global beforeEach, afterEach, describe, expect, it, spyOn, xdescribe, xit, jasmine, DOMClassList */
describe('simple-classList', function () {
    'use strict';

    it('use \'DOMClassList\' as namespace', function () {
        expect(DOMClassList).toBeDefined();
    });
});